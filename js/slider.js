$(document).ready(function(){
	$(".slider").slick({
		slidesToShow:4,
		slidesToScroll:4,
		appendArrows:$(".others-bloggers__slider__frame_item")
	});

	// $(".")
	// slidesToShow:4,
	// slidesToScroll:4
});